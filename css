body { font-family: Arial, sans-serif; margin:0; padding:0; }
header { background: #00aaff; color: white; padding: 1rem; text-align: center; }
nav a { color:white; margin:0 10px; text-decoration:none; }
h2 { text-align:center; margin-top:1rem; }

.route-grid { 
    display:grid; 
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); 
    gap: 20px; 
    padding: 1rem; 
}

.route-card { 
    border:1px solid #ddd; 
    border-radius:10px; 
    overflow:hidden; 
    text-align:center; 
    box-shadow:0 2px 5px rgba(0,0,0,0.1); 
}

.route-card img { width:100%; height:auto; }
.label { display:block; margin-top:5px; font-size:0.9rem; color:#555; }
.price { font-weight:bold; margin:0.5rem 0; }
.btn-group { margin-bottom:1rem; }
.btn { background:#5dade2; color:white; padding:0.5rem 1rem; text-decoration:none; border-radius:5px; margin:0 5px; display:inline-block; }
.btn:hover { background:#3498db; }
.btn-outline { background:none; color:#3498db; border:1px solid #3498db; }
.btn-outline:hover { background:#3498db; color:white; }

footer { background:#333; color:white; text-align:center; padding:1rem; margin-top:2rem; }
